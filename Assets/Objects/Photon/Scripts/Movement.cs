
namespace PhotonInMaze.Photon {
    public enum TouchMovement {
        Left,
        Right,
        Up,
        Down
    };

    public enum MovementEvent {
        Idle,
        Move,
        Teleport,
        ExitFromWormhole
    };
}